#include <stdio.h>

int main() {

    char nome;
    float sexo;
    float contador;
    float totalHomens;
    float totalMulheres;


    totalHomens = 0;
    totalMulheres = 0;

    if(contador = 1 + contador < 56 +  contador){
        printf("Informe o nome da pessoa" ,  contador , ":");
        scanf("%c", nome);

        printf("Informe o sexo da pessoa (M para masculino, F para feminino)");
        scanf("%f", &sexo);

        if(sexo == "M" , sexo == "m"){
          printf(nome + "," , "é homem");
          totalHomens++;
        } else if (sexo == "F" sexo == "f"){
            printf(nome + "," , "é mulher");
            totalMulheres++;
        }else {
            printf("Opcao de sexo invalida.Tente novamente \n");
            contador--;

            return 0;
        }

    }  
}
